{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
        "enabled": {
            "type": "boolean",
            "default": false,
            "description": "Enable Air as the R formatter."
        },
        "air_path": {
            "type": "boolean",
            "default": false,
            "description": "Enable Air as the R formatter."
        }
    }
}
